// Join an array of numbers into a single string, with each number separated by a comma.

let arr = [1, 2, 3, 4, 5];

// let arrayToSingleString = arr.toString();
// console.log(arrayToSingleString);

// Approach 2
let arrayToSingleString = arr.join(", ");
console.log(arrayToSingleString);

