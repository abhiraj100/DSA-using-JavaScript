// Determine the last index of a given object in an array of objects based on a specific property.

let arr = [
    {
        name : "abhiraj",
        age : 22,
    },
    {
        name : "abhiraj",
        age : 22,
    }
]

let newArr = arr.map((el) => {
    return {
        el.lastIndex
    }
})